import { Header } from "../Header/Header";

export const DateSalesComponent = () => {
  return (
    <main className="bg-auth_bg bg-cover bg-center bg-fixed h-screen">
      <div className="sticky top-0 z-50">
        <Header />
      </div>
    </main>
  );
};
